#ifndef AI_SETTINGS
#define AI_SETTINGS

input double BaseLot = 0.01;
input int MaxLayer = 5;
input double MaxSpread = 30;

input bool EnableGhostTrailing = true;
input bool EnableProfitLock = true;
input bool EnableTebasPucuk = true;

input bool EnableAI = true;

#endif
